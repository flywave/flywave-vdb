
SET(ply_HEADER_FILES
  rply.h
  )

SET(ply_SOURCE_FILES
  rply.c
  )

ADD_LIBRARY(ply STATIC
            ${ply_HEADER_FILES}
            ${ply_SOURCE_FILES}
          )

TARGET_INCLUDE_DIRECTORIES(ply PUBLIC $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/..>)

SOURCE_GROUP("src" FILES ${ply_SOURCE_FILES})
SOURCE_GROUP("include" FILES ${ply_HEADER_FILES})

SET_TARGET_PROPERTIES(ply
    PROPERTIES PUBLIC_HEADER "${headers}")

IF(FLYWAVE_ENABLE_SOLUTION_FOLDERS)
  SET_TARGET_PROPERTIES(ply PROPERTIES FOLDER external)
ENDIF(FLYWAVE_ENABLE_SOLUTION_FOLDERS)
